// Lab089_Interview

let ch = 'A';
switch (ch) {
    case 'A': // In JS, we compare the character directly (no ASCII numeric comparison like Java)
        console.log("Match ASCII");
        break;
    default:
        console.log("No Match");
}
