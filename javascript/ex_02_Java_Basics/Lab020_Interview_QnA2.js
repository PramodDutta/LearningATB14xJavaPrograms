// Converted from Java: Lab020_Interview_QnA2.java
// Note: In Java, 'enum' is a keyword and cannot be used as a variable name,
// but 'Enum' (uppercase) can. In JavaScript, 'enum' is a reserved word.

//        let enum = 10;  // Invalid in both Java and JS
let Enum = 10;
//        console.log(enum);
//        let pramod dutta  = 89;  // spaces in variable names are invalid
let pramod$dutta = 90;
let Lab020_Interview_QnA2 = 123;
