// Converted from Java: Lab022_Numerics_Data_Types.java
// Note: JavaScript has only one number type (64-bit floating point).
// Java has byte, short, int, long, float, double, char as separate types.
// The L/l suffix for long and f/F suffix for float are not needed in JavaScript.

let b = 10;        // byte in Java
let s = 10;        // short in Java
let i = 10;        // int in Java
let c = 'A';       // char in Java

let phone = 976543210;   // long in Java (no L suffix needed in JS)
let phone1 = 976543210;  // long in Java (no l suffix needed in JS)

let f = 3.14;      // float in Java (no f suffix needed in JS)
let f2 = 3.14;     // float in Java (no F suffix needed in JS)

let d = 3.12123423; // double in Java
