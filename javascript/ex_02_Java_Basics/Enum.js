// Converted from Java: Enum.java
// In Java this was an empty class named "Enum".
// Note: In Java, "enum" (lowercase) is a keyword, but "Enum" (uppercase) is a valid class name.
// In JavaScript, neither "enum" nor "Enum" are reserved as class names (though "enum" is a reserved word).

class Enum {
}
