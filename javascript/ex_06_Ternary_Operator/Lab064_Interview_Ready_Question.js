// Converted from Java: Lab064_Interview_Ready_Question.java

let age = 21;

// Nested Ternary
// result = condition1 ? expression1 : (condition2 ? expression2 : expression3);

let result = (age > 18) ? (age > 25 ? "You can drink" : "You can go to GOA, ButN can't Drink") : "No";
console.log(result);
