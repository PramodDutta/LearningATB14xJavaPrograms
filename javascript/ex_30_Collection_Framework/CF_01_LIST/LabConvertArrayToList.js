// NOTE: JavaScript Array replaces Java's ArrayList. No type enforcement.
// In JavaScript, arrays and lists are the same thing.

const arr = ["Java", "Python", "C#"];
const list = Array.from(arr); // Convert array to a new array (copy)
console.log(list);
