// NOTE: JavaScript Array replaces Java's ArrayList. No type enforcement.

// List s = new List() - cannot instantiate an interface in Java

const mylist = new Array(5); // Array with initial length hint (not enforced in JS)
const mylist0 = [];
