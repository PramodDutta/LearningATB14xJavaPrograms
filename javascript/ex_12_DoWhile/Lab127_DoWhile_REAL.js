// Lab127_DoWhile_REAL

// Web Automation
let number = 0;
// 0 -> it can be 0 to 10 - elements or students
do {
    console.log(number);
    // callFunction(number);
    // close the excel file, open the file.
    number++;
} while (number < 0);
