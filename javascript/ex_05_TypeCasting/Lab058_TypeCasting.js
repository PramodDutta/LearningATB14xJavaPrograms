// Converted from Java: Lab058_TypeCasting.java
// Note: JavaScript has only one number type (64-bit float), so widening/narrowing
// between byte, short, int, long is not applicable. All numeric assignments are straightforward.

let b = 10;
let a = b;          // Valid Syntax -> Widening -> Implicit Casting - Automatically done.
let a1 = Number(b); // Valid Syntax -> Widening -> Explicit Casting - not required
