// Converted from: ex_19_OOPs_Part2/OOPs_01_inheritance/multipleheritance/Son.java

/**
 * Java does not support multiple inheritance with classes.
 * In the original Java code, "extends Father1, Father2" is commented out
 * because it is not allowed.
 *
 * JavaScript also does not support multiple inheritance directly.
 * However, it can be achieved using mixins pattern if needed.
 * Example:
 *   const Father1Mixin = (Base) => class extends Base { ... };
 *   const Father2Mixin = (Base) => class extends Base { ... };
 *   class Son extends Father1Mixin(Father2Mixin(Object)) { ... }
 */

class Son {
    // extends Father1, Father2 is not supported in Java or JS
}

// main
// money()

module.exports = Son;
