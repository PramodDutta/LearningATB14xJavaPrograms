// Converted from Java: Lab033_Literals.java
// Note: JavaScript supports the same numeric literal formats as Java:
// binary (0b), octal (0o), and hexadecimal (0x).
// However, Java's octal format uses a leading 0 (e.g., 0101), while
// JavaScript's strict mode uses 0o prefix. Leading 0 also works in non-strict mode.

let age = 65; // Decimal System - base will be 10

// Binary Literal
let binary_num = 0b1010; // Binary System

// Octal base? -> 8
let octal = 0o101; // Using 0o prefix (JS best practice, equivalent to Java's 0101)

// Hexadecimal Literals - 16
let hex = 0xface;
// base 16 color combinations are in hex - #28a745
